mixin icon(name, width, height)
  svg(width=width, height=height, aria-hidden="true")&attributes(attributes)
    use(xlink:href="/img/sprite.svg#" + name)

//- mixin icon(name, width, height)
//-   img(src="img/sprite/" + name + ".svg", width= width, height= height)&attributes(attributes)

//- &attributes(attributes) позволяет вызывать миксин с доп классом и аттрибутами
//- +icon("icon-close", 15, 15).some-class
