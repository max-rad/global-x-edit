mixin articles(data)
  section.articles
    .container
      .articles__wrapper
        h1.title.title--h1!=data.title
        ul.articles__list
          each item in data.list
            li.articles__item
