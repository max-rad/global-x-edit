mixin map()
  section.map
    .container
      .map__wrapper
        h2.title.title--t1 Городские видеокамеры
        .map__map-wrapper
          .map__map
            +img("content", "map", "png", 1440, 615, "map")
